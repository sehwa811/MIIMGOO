export const selectImgObj = (state:any) => state.image.imgObj;

export const selectFormData = (state:any) => state.image.formData;

export const selectImgTitle = (state:any) => state.image.imageTitle;