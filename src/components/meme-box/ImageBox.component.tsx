

import styled from "styled-components";

const Wrapper = styled.div`
  width: 10rem;
  height: 12.5rem;
  display: flex;
  flex-wrap: wrap;

  img {
    width: 10rem;
    height: 12.5rem;
    object-fit: cover;
  }
`;

const ImageBox = () => {
  return (
    <Wrapper>
      
    </Wrapper>
  );
};

export default ImageBox;
